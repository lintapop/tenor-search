export declare class ResourceManager {
    private handlePool;
    private handles;
    private getOrCreateGlobalHandle;
    load: <T>(key: string) => Promise<T>;
    resolve: <T>(key: string, value: T) => void;
    immediate: <T>(key: string) => T | null;
    remove: (key: string) => void;
}
