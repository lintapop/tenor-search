export declare class StateManager {
    private handlePool;
    private groups;
    private globals;
    private getOrCreateHandle;
    private getOrCreateGlobalHandle;
    load: <T>(entityId: number, key: string) => Promise<T>;
    resolve: <T>(entityId: number, key: string, value: T) => void;
    immediate: <T>(entityId: number, key: string) => T | null;
    remove: (entityId: number, key: string) => void;
    removeAll: (entityId: number) => void;
    loadGlobal: <T>(key: string) => Promise<T>;
    resolveGlobal: <T>(key: string, value: T) => void;
    immediateGlobal: <T>(key: string) => T | null;
    removeGlobal: (key: string) => void;
}
